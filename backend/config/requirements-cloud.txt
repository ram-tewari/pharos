# Cloud API requirements (Render Free Tier - 512MB RAM)
# Extends base requirements with cloud-specific dependencies
# DO NOT duplicate base dependencies here - they are inherited automatically

-r requirements-base.txt

# Database
psycopg2-binary==2.9.10
aiosqlite==0.20.0  # For SQLite async support

# Vector Database
qdrant-client==1.12.1

# Note: NO torch, transformers, or heavy ML libraries
# Cloud API is lightweight and dispatches ML tasks to Edge Worker
