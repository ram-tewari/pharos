# Edge Worker requirements (Local GPU - RTX 4070/16GB RAM)
# Extends base requirements with full ML dependencies
# DO NOT duplicate base dependencies here - they are inherited automatically

-r requirements-base.txt

# Database (same as cloud for consistency)
psycopg2-binary==2.9.9

# Vector Database
qdrant-client==1.7.0

# Deep Learning
torch==2.1.0
torch-geometric==2.4.0

# Code Parsing
tree-sitter==0.20.4
tree-sitter-python==0.20.4
tree-sitter-javascript==0.20.3

# Scientific Computing
numpy==1.24.3

# Note: Heavy ML dependencies for graph neural networks and code parsing
# Edge Worker leverages local GPU for compute-intensive operations
