# Pharos Cloud Requirements
# Cloud-specific dependencies (no GPU dependencies)
# Optimized for Render + NeonDB + Upstash serverless stack

-r requirements-base.txt

# ============================================================================
# MISSING DEPENDENCIES (CRITICAL)
# ============================================================================

# Web scraping (required by resources module)
beautifulsoup4>=4.12.0
lxml>=4.9.0

# URL slugification (required by resources module)
python-slugify>=8.0.0

# JSON schema validation (required by mcp module)
jsonschema>=4.20.0

# Upstash Redis REST API (required for edge worker task queue)
upstash-redis>=0.15.0

# ============================================================================
# SERVERLESS DATABASE DRIVERS
# ============================================================================

# PostgreSQL (NeonDB-compatible)
psycopg2-binary>=2.9.9  # Sync driver for background tasks
# asyncpg>=0.29.0  # Already in requirements-base.txt

# ============================================================================
# SERVERLESS REDIS
# ============================================================================

# Upstash Redis (serverless, pay-per-request)
# Note: Standard redis package (already in requirements-base.txt) works with Upstash

# ============================================================================
# EMBEDDINGS (CLOUD MODE - NO LOCAL MODELS)
# ============================================================================

# CRITICAL: DO NOT install sentence-transformers in CLOUD mode!
# It pulls in PyTorch (~500MB) which causes OOM on Render Starter (512MB RAM)
#
# sentence-transformers>=2.2.0  # ❌ DISABLED - causes OOM on Render
#
# Cloud mode architecture:
# - Render API: Queues embedding tasks to Upstash Redis
# - Edge Worker: Processes tasks with local GPU (RTX 4070)
# - No ML models loaded on Render instance
#
# For cloud-based embeddings (alternative to edge worker):
# - OpenAI embeddings API: openai>=1.0.0 (already included below)
# - Cohere embeddings API: cohere>=4.0.0
# - Voyage AI embeddings API: voyageai>=0.1.0

# ============================================================================
# LLM APIS (OPTIONAL)
# ============================================================================

# OpenAI (for cloud-based embeddings/LLM if needed)
openai>=1.0.0

# Anthropic (for cloud-based LLM if needed)
anthropic>=0.7.0

# ============================================================================
# VECTOR STORAGE (OPTIONAL)
# ============================================================================

# Qdrant Cloud (for vector storage, alternative to pgvector)
# qdrant-client>=1.7.0  # Uncomment if using Qdrant instead of pgvector

# ============================================================================
# CLOUD UTILITIES (OPTIONAL)
# ============================================================================

# AWS SDK (optional, for S3 storage)
# boto3>=1.33.0

# Google Cloud Storage (optional, for GCS storage)
# google-cloud-storage>=2.14.0

# ============================================================================
# PRODUCTION MONITORING (OPTIONAL)
# ============================================================================

# Prometheus metrics
prometheus-client>=0.20.0
prometheus-fastapi-instrumentator>=7.0.0

# Sentry (error tracking)
# sentry-sdk>=1.40.0

# Datadog (APM and monitoring)
# ddtrace>=2.0.0

# New Relic (APM and monitoring)
# newrelic>=9.0.0

# ============================================================================
# NOTES
# ============================================================================

# Memory Usage (Render Starter: 512MB RAM):
# - Base dependencies: ~100MB
# - Sentence Transformers: ~200MB (model loaded)
# - FastAPI + Gunicorn: ~100MB
# - Headroom: ~100MB
# - Total: ~500MB (safe)

# If you see OOM errors:
# 1. Reduce WEB_CONCURRENCY to 1
# 2. Use OpenAI embeddings API instead of local models
# 3. Upgrade to Render Standard (2GB RAM)

# Connection Pool Sizing:
# - Pool size: 3 base + 7 overflow = 10 per worker
# - 2 workers × 10 connections = 20 total
# - NeonDB free tier: 100 connections (safe)

# Upstash Redis:
# - Free tier: 10,000 requests/day
# - Sufficient for solo dev (<100 requests/min)
# - Upgrade to Pro ($10/mo) for 1M requests/mo